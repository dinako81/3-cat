{
  "name": "3-cat",
  "version": "1.0.0",
  "description": "Cat projekt",
  "main": "index.js",
  "scripts": {
    "dev": "dead-server --host=localhost --port=4203",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "html"
  ],
  "author": "Dina",
  "license": "ISC",
  "devDependencies": {
    "dead-server": "^1.0.9"
  }
}
